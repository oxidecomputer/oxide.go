package main

func main() {
	/*
		httpClient := &http.Client{
			Transport: http.DefaultTransport,
		}
		fmt.Println("### Before ###")
		spew.Dump(http.DefaultTransport)

		client, err := oxide.NewClient(
			oxide.WithHTTPClient(httpClient),
			oxide.WithInsecureSkipVerify(),
		)
		if err != nil {
			println(err.Error())
			os.Exit(1)
		}
		//fmt.Println("After")
		//spew.Dump(http.DefaultTransport)

		//fmt.Println("Client")
		//spew.Dump(httpClient)

		ctx := context.Background()
		err = client.LoginSaml(ctx, oxide.LoginSamlParams{
			ProviderName: "google",
			SiloName:     "oxide",
		})
		if err != nil {
			println(err.Error())
			os.Exit(1)
		}
	*/
}
