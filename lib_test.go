package oxide

/*import (
	"testing"
)

func getClient(t *testing.T) *Client {
	client, err := NewClientFromEnv("oxide.go/tests")
	if err != nil {
		t.Fatalf("creating the client failed: %v", err)
	}
	return client
}*/
